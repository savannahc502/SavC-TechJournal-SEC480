# Varibles for the Wazuh SIEM Alert Simulation 

# Creating a new user for the persistence simulation
testUser: "Faker_luser"

# Directory for simulation artifacts
testDirectory: "/tmp/wazuh_sim"

# Log file for shadow access simulation
shadowAttemptLog: "/tmp/wazuh_sim/shadow_attempt.log"

# Malicious script pathing 
malScriptPath: "/tmp/wazuh_sim/mal_sim.sh"

# Ports for nmap scaning simulation
nmapPorts: "22,80,443,3306,5432"

# The target for Nmap (the loopback address)
nmapTarget: "127.0.0.1"

# Fake syslog entry to display log tampering simulation
fakeLogEntery: "Apr-22 localhost sudo: hehe Get Attacked"

# Targeted paths by FIM simulation
fimTestFile: "/etc/wazuh_fim_test.conf"

# Authentication log path for tamper simulation
authLogPath: "/var/log/auth.log"

# Syslog pathing for injection simulation
syslogPath: "/var/log/syslog"